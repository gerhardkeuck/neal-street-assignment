# ansible/ansible.cfg
[defaults]
inventory = inventories/dev.aws_ec2.yaml
roles_path = roles
host_key_checking = False
interpreter_python = auto_silent
timeout = 60
retry_files_enabled = False

[inventory]
enable_plugins = amazon.aws.aws_ec2
